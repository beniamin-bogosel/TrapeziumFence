# Documentation

This folder contains the LaTeX documentation for the `TrapeziumGPT` verifier.

Build with:

```sh
make
```

The main source is `main.tex`.  The PNG figure used by the experiment section is
stored in `figures/current_survivor_rectangles.png`.
