# Documentation

This folder contains the LaTeX documentation for the `TrapeziumFence` verifier.

Build with:

```sh
make
```

The main source is `main.tex`.  The PNG figure used by the experiment section is
generated by `make_survivor_rectangles.py` and stored in
`figures/current_survivor_rectangles.png`.
